import { colors } from "./colors"
import * as constants from "../default/constants"

const root = {
  name: "Blue",
  version: "0.0.1",
}

export const BlueTheme = {
  ...root,
  constants,
  colors,
}
